<<<<<<< HEAD
name: CI - Backend & Frontend (matrix ready)

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  backend:
    name: Backend - Java (Maven)
    runs-on: ubuntu-latest

    # MATRIX cho Java – hiện tại chỉ dùng 21,
    # sau này chỉ cần thêm '17', '22', ... vào đây
    strategy:
      matrix:
        java-version: [ '21' ]

    defaults:
      run:
        working-directory: ./backend
=======
name: CI - Java + JS App

on:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:

jobs:
  backend-ci:
    name: Backend - Java (Maven)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
>>>>>>> a3f08c770f17e6bfa3bf841a2bb08b96e18afdc7

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java-version }}
          cache: maven

      - name: Build & test backend (Maven)
        run: mvn -B clean verify

  frontend:
    name: Frontend - React (npm)
    runs-on: ubuntu-latest
    needs: backend

    # MATRIX cho Node – hiện tại 1 version, sau này dễ thêm
    strategy:
      matrix:
        node-version: [ '18' ]

    defaults:
      run:
        working-directory: ./frontend
=======
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Build & test with Maven
        run: mvn -B clean verify

  frontend-ci:
    name: Frontend - JavaScript (React)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
>>>>>>> a3f08c770f17e6bfa3bf841a2bb08b96e18afdc7

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies (npm ci)
        run: npm ci

      - name: Run frontend tests
        run: npm test -- --watch=false

      # Tạm thời build với CI=false để không bị fail vì ESLint warning
      - name: Build frontend
        run: CI=false npm run build
=======
      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'frontend/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --watch=false
        # nếu bạn chưa viết test, có thể tạm comment lại dòng này

      - name: Build frontend
        run: npm run build
>>>>>>> a3f08c770f17e6bfa3bf841a2bb08b96e18afdc7
